// Re-export colors from theme for backward compatibility
import { colors as themeColors } from '../utils/theme';

// Export the colors directly
export const colors = themeColors;

// Default export
export default colors; 